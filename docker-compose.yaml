services:
  meshmap:
    build: ./backend
    container_name: meshmap-live
    env_file:
      - path: .env
        required: false
    ports:
      - "${WEB_PORT:-8080}:8080"
    restart: unless-stopped
    volumes:
      - ./data:/data
      - ./backup:/backup
      - ./:/repo:ro
